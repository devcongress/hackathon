<%= content_for :title, "Join #{@team.name} | UNICEF StartUp Lab Hack" %>

<div class="mx-auto max-w-3xl px-4 py-8 sm:py-16">
  <div class="bg-white rounded-2xl shadow-md border border-primary-100/50 px-6 py-12 sm:px-12">
    <!-- Header -->
    <div class="text-center">
      <h1 class="space-y-2">
        <span class="block text-3xl sm:text-5xl font-light text-gray-800">You've Been Invited!</span>
        <span class="block text-4xl sm:text-6xl font-bold bg-clip-text text-transparent bg-gradient-to-r from-primary-800 to-primary-600"><%= @team.name %></span>
      </h1>

      <!-- Theme Badge -->
      <div class="mt-6">
        <span class="inline-flex items-center rounded-full bg-primary-100 px-4 py-1 text-base font-medium text-primary-700">
          Imagining the Future of Finance with AI
        </span>
      </div>

      <!-- Invitation Message -->
      <div class="mt-10 space-y-6">
        <p class="text-lg sm:text-xl text-gray-700 leading-relaxed">
          <span class="font-medium text-gray-900"><%= @team.hacker.name %></span>
          wants you to join their team
          <span class="font-medium text-gray-900"><%= @team.name %></span>
          for an incredible hackathon experience!
        </p>

        <!-- Event Details -->
        <div class="mt-8 grid grid-cols-1 sm:grid-cols-2 gap-6 text-left max-w-xl mx-auto">
          <div class="flex items-start space-x-3">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-primary-600 mt-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z" />
            </svg>
            <div>
              <h3 class="font-semibold text-gray-900">When</h3>
              <p class="mt-1 text-gray-600">March 14-16, 2025</p>
            </div>
          </div>
          
          <div class="flex items-start space-x-3">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-primary-600 mt-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z" />
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z" />
            </svg>
            <div>
              <h3 class="font-semibold text-gray-900">Where</h3>
              <p class="mt-1 text-gray-600">Academic City Campus</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Prize Highlight -->
      <div class="mt-12 bg-gradient-to-br from-primary-50 to-primary-100 rounded-xl p-6 max-w-xl mx-auto">
        <div class="flex items-center justify-center space-x-3">
          <svg xmlns="http://www.w3.org/2000/svg" class="h-8 w-8 text-primary-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />
          </svg>
          <div class="text-xl font-semibold text-primary-800">₵30,000+ in Prizes</div>
        </div>
      </div>

      <!-- Challenge Text -->
      <p class="mt-12 text-2xl sm:text-3xl font-medium text-gray-800 leading-relaxed">
        Ready to join the innovation?
      </p>

      <!-- Actions -->
      <div class="mt-12 flex flex-col items-center space-y-6">
        <!-- Accept Button -->    
        <%=
          button_to "Let's Hack!",
                    accept_invitation_path,
                    class: "group w-64 rounded-xl bg-gradient-to-r from-primary-600 to-primary-500 px-6 py-4 text-lg font-semibold text-white shadow-md transition-all duration-300 ease-out hover:translate-y-[-2px] hover:shadow-lg focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2"
        %>

        <!-- Decline Link -->
        <%=
          link_to "Decline Invitation",
                  decline_invitation_path,
                  class: "text-base text-gray-500 hover:text-gray-700 font-medium underline decoration-gray-300 underline-offset-4 hover:decoration-gray-500 transition-colors duration-200",
                  data: { 
                    turbo_method: :post,
                    turbo_confirm: "Are you sure you want to decline this invitation? You can still register with a different team later."
                  }
        %>
      </div>
    </div>
  </div>
</div>